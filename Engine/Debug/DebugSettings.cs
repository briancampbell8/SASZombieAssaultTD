namespace SASZombieAssaultTD.Engine.Debug
{
    public static class DebugSettings
    {
        // When true, the engine will generate JSON tables during initialization.
        public static bool GenerateTables = true;

        // When true, the asset loader will skip loading images and return a placeholder.
        public static bool SkipImageLoading = true;
    }
}
